web.rar
This is main web app, kindly extract its contents and upload it in your local web server or online and perform below steps:

1. .htaccess -> mention the folder name in the file if it is sub app or subdomain app.

2. application/config.php -> add proper url of the application to be accessed.

3. application/database.php -> add proper database credentials of your dbms where you have imported the attached database.

EKFC-CLIENT
use this app to upload the movies/trailers from any machine/ desktop without requiring any web server, before starting you need to configure its form action url to your web server url.


